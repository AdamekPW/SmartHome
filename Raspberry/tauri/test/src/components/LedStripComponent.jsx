import styles from "../styles/components/LedStripComponent.module.scss";

const LedStripComponent = () => {
    return <p className={styles.doUsuniecia}>Pasek LED</p>;
};

export default LedStripComponent;
